import mongoose from 'mongoose'; import dotenv from 'dotenv'; dotenv.config(); const MONGO_URI = 'mongodb+srv://asadzadehali09_db_user:7L0YmBBMKfGHoYKZ@cluster.hpsurio.mongodb.net/af_hotel?appName=Cluster'; const SettingsSchema = new mongoose.Schema({ tiktok: String }, { strict: false }); const Settings = mongoose.models.Settings || mongoose.model('Settings', SettingsSchema); async function run() { await mongoose.connect(MONGO_URI); console.log('Connected'); const res = await Settings.findOneAndUpdate({}, { tiktok: 'https://www.tiktok.com/@afhotel.az' }, { new: true, upsert: true }); console.log('Updated settings:', res); process.exit(0); } run();
