import mongoose from 'mongoose'; import dotenv from 'dotenv'; dotenv.config(); const MONGO_URI = 'mongodb+srv://asadzadehali09_db_user:7L0YmBBMKfGHoYKZ@cluster.hpsurio.mongodb.net/af_hotel?appName=Cluster'; const SettingsSchema = new mongoose.Schema({ facebook: String, tiktok: String }, { strict: false }); const Settings = mongoose.models.Settings || mongoose.model('Settings', SettingsSchema); async function run() { await mongoose.connect(MONGO_URI); const res = await Settings.findOne({}); console.log('Facebook:', res.facebook); console.log('TikTok:', res.tiktok); process.exit(0); } run();
